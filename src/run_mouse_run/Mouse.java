package run_mouse_run;


public class Mouse extends CharacterController
{
    public Mouse(GameManager gameManager, String name, Position initialPosition)
    {
        super(gameManager, name, initialPosition);
    }
}
